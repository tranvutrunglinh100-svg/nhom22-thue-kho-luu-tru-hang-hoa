# WarehouseMS - Hệ thống Quản lý Kho Lưu Trữ (Phiên bản A)

## Hướng dẫn chạy

1. Cài đặt thư viện:
vào file bấm shift + chuột phải chọn open powershell windown here.
   ```bash
   pip install -r requirements.txt

2. chạy ứng dụng:
   python app.py